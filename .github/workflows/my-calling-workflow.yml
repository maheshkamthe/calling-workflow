name: my calling workflow
on: push

jobs:
  main_workflow_job:
    uses: maheshkamthe/called-workflow/.github/workflows/my-called-workflow.yml@main
    with:
        first-name: mahesh
    secrets: inherit
    #steps:
        #- uses: actions/checkout@master
        #- name: call reusable workflow in other repo using dockerfile
          #uses: maheshkamthe/called-workflow@main
          #with:
            #first-name: mahesh

  #      - name: call reusable workflow in same repo
  #        uses: /calling-workflow/.github/workflows/my-called-action.yml@main
  #        with:
  #          first-name: mahesh


